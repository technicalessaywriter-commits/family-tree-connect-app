{
  "name": "familia-connect",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace apps/api\" \"npm run dev --workspace apps/web\"",
    "dev:mobile": "npm run start --workspace apps/mobile",
    "android": "npm run android --workspace apps/mobile",
    "ios": "npm run ios --workspace apps/mobile",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspace apps/api && npm run test --workspace apps/mobile && npm run test --workspace apps/web",
    "lint": "npm run lint --workspaces",
    "seed": "npm run seed --workspace apps/api"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
